#include<iostream>
#include "tt.h"
#include "tasktree.h"
#include "fstream"

int main()
{
	Main();
	/*std::vector<Node*> list = Read_and_extract();
	show(list);
	Sleep(5);
	std::string command;
	std::getline(std::cin, command);
	edit_Task(list, command);
	show(list);
	Sleep(100);*/

}